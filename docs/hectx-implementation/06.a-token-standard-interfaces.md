**Abstract**
Concrete interface implementations for Splice Token Standard.

**Body**
I. Holding Interface Implementation
```
interface instance Api.Token.HoldingV1.Holding for HectXHolding where
  view = Api.Token.HoldingV1.HoldingView with
    owner
    instrumentId = instrumentId
    amount = amount
    lock = None
    meta = Api.Token.MetadataV1.Metadata with values = TextMap.fromList ...
```

II. TransferFactory Interface Implementation
```
interface instance Api.Token.TransferInstructionV1.TransferFactory for HectXTransferFactory where
  view = Api.Token.TransferInstructionV1.TransferFactoryView with
    admin
    instrumentId
    meta = emptyMeta
```

**References**
- `hectx-daml/daml/HectX/Holding.daml`
- `hectx-daml/daml/HectX/Transfers.daml`
